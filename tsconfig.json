{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // Los tests corren en Node (tsx --test), no en el móvil.
    "types": ["node", "react"]
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules", "scripts/tmp"]
}
